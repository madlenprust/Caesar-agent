"""
Автономный AI-агент для Ubuntu без GUI.

Главный принцип: пользователь даёт задачу → агент молча уходит работать →
возвращается с готовым результатом. Никаких «хочешь, продолжим?».

Архитектура: daemon + thin CLI client через unix socket.
Память: L1 (RAM) / L2 (SQLite) / L3 (vector) / L4 (skills).
LLM-роутер: дешёвая анализирует, умная отвечает.
"""

# Версия: MAJOR.MINOR.PATCH
# - PATCH (0.1.0 → 0.1.1 → ... → 0.1.99): каждый фикс/фича
# - MINOR (0.1.99 → 0.2.0): крупные изменения
# - MAJOR (0.99.x → 1.0.0): breaking changes
# Обновляется ВРУЧНУЮ при каждом коммите.
__version__ = "0.7.4"
